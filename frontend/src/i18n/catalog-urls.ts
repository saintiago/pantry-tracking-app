import es from './locales/es.json?url&no-inline';
import it from './locales/it.json?url&no-inline';

// URLs only: Vite emits separate, hashed files without fetching or inlining their contents.
export const catalogUrls = { es, it };
