{
  "name": "pantry-tracking-app",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/domain",
    "infrastructure",
    "backend",
    "frontend"
  ],
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run test --workspaces --if-present",
    "test:unit": "npm run test:unit --workspaces --if-present",
    "test:property": "npm run test:property --workspaces --if-present",
    "test:e2e": "playwright test",
    "test:e2e:production": "playwright test --config playwright.production.config.ts",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "audit:inventory": "npm run build:domain && npm run build --workspace backend && node scripts/audit-inventory.mjs",
    "test:inventory:aws": "npm run build:domain && npm run build --workspace backend && node scripts/test-inventory-aws.mjs",
    "migrate:inventory-groups": "npm run build:domain && npm run build --workspace backend && node scripts/migrate-inventory-groups.mjs",
    "type-check": "npm run type-check --workspaces --if-present",
    "prepare": "npm run build:domain && husky",
    "build:domain": "npm run build --workspace @pantry/domain",
    "pretest": "npm run build:domain",
    "pretest:unit": "npm run build:domain",
    "pretest:property": "npm run build:domain",
    "pretype-check": "npm run build:domain",
    "check:architecture": "node scripts/check-architecture.mjs",
    "check:docs": "node scripts/check-docs.mjs",
    "verify:static": "npm run type-check && npm run lint && npm run check:architecture && npm run check:docs",
    "verify": "npm run verify:static && npm test && npm run build --workspace frontend && npm run verify:bundle --workspace frontend && npm run test:e2e && npm run test:e2e:production"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^8.57.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.3.0",
    "typescript": "^5.5.0"
  }
}
